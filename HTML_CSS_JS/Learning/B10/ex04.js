// Toán tử
/**
 * Toán tử số học (+ - * / % ** ++ --)
 * Toán tử gán (= += -= *= /= %=)
 * Toán tử so sánh (== === != !== < > <= >=)
 * Toán tử logic (&& || !)
 */

// let a = 10;

// console.log(a++); //Hậu tố
// console.log(++a); // Tiền tố

// let x = 10;
// console.log(x++ + ++x - --x + x--);

// // Toán tử gán
// let a = 10;
// a **= 10;
// console.log(a);

// Toán tử so sánh

/**
 * Ép kiểu ngầm định và ép kiểu chủ động
 *
 */

//Toán tử logic
/**
 * &&: toán tử AND
 * ||: toán tử OR
 * !: toán tử phủ định (NOT)
 */
console.log(true && false);
console.log(true || false);
console.log(false || true);

/**
 * Falsy values: fale, 0, "", null, undefined, NaN
 */
console.log("hoang" && "huy" && undefined);
