// const arr1 = [1, 0, 2, 4, 9, 0, 6, 8, , , , 9, "abc"];
// const arr2 = [0, 6, 7, 9, 6, undefined, 10, null, "123"];

// //Lọc ra các phần tử trùng lặp
// // nếu xuất hiện từ 2 lần thì chỉ đếm 1
// //kh đc tính các gtri falsy và ompty (trừ số 0 )

// function filterArr(arr1, arr2) {
//   const setArr1 = new Set(arr1);
//   const setArr2 = new Set(arr2);

//   const filter = [...setArr1].filter(
//     (item) => setArr2.has(item) && (item || item === 0)
//   );

//   console.log(filter);
// }
// filterArr(arr1, arr2);

//Xây dựng hàm sortar(field) để sắp xếp theo trường dữ liệu field
//**
// 1. nếu field là string thì sawxp xếp từ a-z
// 2. Nếu field là number hoặc datetime thì sắp xếp tăng dần */

function sortProducts(arr, field) {}
sortProducts(products, price);
